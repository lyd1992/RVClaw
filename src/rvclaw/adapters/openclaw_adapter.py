class OpenClawAdapter:
    """Placeholder boundary for the post-v0.1 OpenClaw adapter."""

    def __init__(self, *args, **kwargs):
        raise NotImplementedError("OpenClaw adapter is planned after the mock device contract is frozen.")
